Generated from [[26-09-20_sarimaxResidualBoostingCase]] — iteration 1 (batch 20)

# Batch 20 — the release: the manuscript, the reproducibility report, the safety scan, and both checks run again

Row 20 asked for three things: the manuscript sections this project supports, the release scan,
and the push. The first two are done, together with what `/release` says must precede anything
counting as released — the clean-room reproduction and the outsider test run again at the
release commit, the hierarchical report rebuilt, and the reproducibility report written. **The
push is not made.** It is the one step in this repository that cannot be undone, the release
skill says never to make it without asking, this batch ran without the human present, and the
scan produced findings the human should see first (§2, §8).

## 1. The manuscript

`Human-AI-collaboration/manuscript/26-09-23_twoStageDengueLaosManuscript.md`, about 7,200 words,
with a provenance sidecar of 127 statement rows. It is written from all 32 claims — 85 rows rest on
a named claim, 37 on a stored file that fixes a design fact, 5 on the batch-10 literature record or
on stated inference. Structure: abstract; an introduction placing the design in the
residual-correction and stacking literature; data; methods, covering the evaluation and its
verified metric, stage 1, the baselines, the ten stage-2 candidates and the two errors they were
trained on, the diagnostics, the pre-registration rule, the stability sets, the held-out design,
and a section on the method of work; results on development and on the held-out year with two
tables; discussion; limitations; reproducibility and data availability; an agency statement;
references.

Two questions the task log had left for the human were answered by default so the row would not
block: the manuscript carries the method-of-work section, because plan §1 makes the veridical
record a deliverable of equal standing with the model; and no venue's constraints were applied,
because none is decided. Both are reversible by a `_v2` iteration.

The eight references were re-verified by web search. One characterisation was wrong in the
batch-10 record and would have been wrong in the article: Sesay et al. (2026) was summarised as a
negative-binomial autoregression reaching near-nominal coverage "where a GLM did not"; the
article's abstract says the calibration belongs to a negative-binomial integer autoregression
against a negative-binomial GLM that substantially under-covered, and that lag-1 climate added no
significant skill. The manuscript says the latter. The other seven papers were not re-read; the
manuscript characterises them as the batch-10 record does, and the provenance section says so.

`/claims check-text` flags 77 sentences; each was read. All are method, literature or
interpretive statements mapped in the sidecar, reference fragments, or the agency statement. One
imprecision was found by reading them — which nodes read the climate fields — and corrected.

## 2. The release scan

`AI-internal/useful-scripts/release_scan.sh` did not exist; `/release` described a scan nothing
performed, the class of defect batch 19 found in `/validate cleanroom`. It exists now, writes to
`AI-generated/validation/2026-09-23_release-scan/`, and fails automatically on exactly two
things: a secret-shaped string or a credential-shaped filename. Everything else it lists for a
reader.

| Question | Finding |
|---|---|
| Secret-shaped strings in tracked files at HEAD | **0** |
| Secret-shaped strings in every line ever added, 269 commits | **0** (22 added lines over 4,000 characters — the archived boundary polygons — skipped as data and counted) |
| Credential-shaped filenames anywhere in the history | **1** — `.claude/settings.local.json`, tracked from `1f0a147` (the prior project's first commit, 2026-08-23) to `15b4ba9`. Content: three permission grants, one naming an absolute path under the author's home directory. **No secret.** |
| Home-directory paths and e-mail addresses in tracked files | 13 lines in four files, all one path: the batch-19 clean-room console log, task entry T16, plan §4b (2026-09-23), `readme-at-start.md`'s settings table |
| Imported data files and their governance statements | 7, in three `Archive/` folders |
| Tracked files over 5 MB | 1 (the Lao boundary GeoJSON, 8.2 MB) |

**The one automatic failure, examined.** The scan is right to fail on the filename and a reader
is right to check the content; the content is not a credential. The history is not rewritten:
every provenance record, freeze record and opening record cites commit hashes, and the file has
been on the public remote since 2026-08-23. The verdict stands in `summary.json` as `failed`,
with this examination beside it, because a scan that is edited to pass is decoration.

**The home-directory path** discloses nothing the history does not: the same username is the
author name on every commit since the first push, and the e-mail is in the same metadata
(`git_identities.txt`). It is reported and not redacted — two of the four files are append-only
records and one is a generated artefact — and left to the human, who may prefer redaction in the
two prose files.

**Data permission** was checked upstream rather than asserted. The source repository
`dhis2/climate-health-data` carries no licence file; its README describes data published for
direct download; the schema attributes the case counts to OpenDengue, whose site states
CC BY 4.0. Attribution is appended to `Archive/lao-dataset/provenance.md` and
`Archive/sibling-datasets/provenance.md`. The ERA5-Land, OCHA COD-AB and WorldPop terms for the
covariate fields were not checked; those fields are on no main-path model.

## 3. The clean-room reproduction, at the release commit

Run again rather than carried over from batch 19, because two scripts under `analysis/`
(`02_plan_manifest.py`'s error messages, `10_report_holdout.py`'s stopwatch fix) and every
`run.sh` changed after that run. A fresh clone, both environments built from nothing,
`analysis/run.sh` in full, every tracked result compared byte for byte.

| | |
|---|---|
| Cloned at | `0642af0`, the commit this batch started from |
| Tracked inputs differing on checkout | **0** |
| `analysis/run.sh` | exit 0 after 4,757 s |
| Results byte-identical | **289** |
| Results declared-varying (wall-clock, timestamps, the clone's own opening record) | 10 |
| Results genuinely differing | **0** |
| Results missing in the clone | **0** |

**Everything comparable reproduces, held-out rows included**, with no fix needed this time. The
clone's own `/validate invariants` passed on every check but `git`, which failed on the files its
run had just written — the behaviour `validate.md` documents. Artefacts in
`AI-generated/validation/2026-09-23_cleanroom-artefacts/`.

One thing to know when reading `summary.json` there: its `head` field says `e5dd305`, not
`0642af0`. `cleanroom.sh` reads HEAD when it writes the summary, at the end, and this batch's
commits landed while the clone was running; the clone itself was of `0642af0`, as the console log
records. The script is left as it is in this batch — the artefact that passed was produced by the
committed script — and the defect is recorded for the human: the summary should record the commit
that was cloned, captured at clone time.

## 4. The outsider test, at the release commit

A fresh agent with no context, given the repository and nine questions with checkable answers.
It found both interpreters, established the main path three independent ways, stated what may
be done with the held-out year and where the opening is recorded, traced the manuscript's
held-out headline from the sentence through the sidecar to claim C14, to `conclusion.json`, to
the provenance record, to the script and invocation, verified twelve recorded digests against the
current files, and recomputed both means from the 192 scored rows of the per-cell file to match
99.20 and 128.51.

It found fourteen false or stale statements. Nine are fixed, three were in-progress states of
this batch, two are recorded (`AI-generated/validation/2026-09-23_outsider-test-release.md`).
The one that matters:

**Six provenance records still named the batch-2 lockfile digest**, fifteen batches after batch
5 changed the lockfile, and the `hashes` invariant had never noticed, because it verified the
digests in `script:` blocks and not the one in the `environment:` block. `validate.md`'s "every
digest a record gives is the file's current one" was true as prose and false as behaviour — the
same class as batch 16's `freeze` check, batch 19's `cleanroom.sh` and this batch's
`settings.json`. Fixed at both ends: each record has the section it owed (when the environment
changed, why, and that the result was reproduced byte for byte under the current lockfile by
both clean-room runs), and `check_hashes` now verifies the lockfile digest too. The extended
check was shown to fail on a stale record before the records were brought up to date, and to
pass after.

The rest: the archive README named the prior project's node (`01_partition`), corrected by
appending; `claims.md`'s header still said "No claims yet"; `node.md`'s usage line gave a form
the script rejects; no skill said to read `provenance-record.md` and `AGENTS.md` described
`skill-references/` as holding more than it does; plan §4 pointed at that file for the seed
construction, which lives in `analysis/scripts/lib/project_seed.py`; `04_stage2/claim.md`'s
first answers paragraph read as current while naming a superseded main path, and
`readme-at-start.md`'s status did the same twenty lines apart — both now lead with the state in
force. **Recorded and not fixed:** a new alternatives child added today has no mechanised route
to a new development-manifest version while the main path is unchanged; batch 19 named the dead
end in the tooling's messages, and mechanising the route is a planner change a release batch
should not make.

## 5. The reproducibility report, and what it is written from

`AI-generated/repro-report/26-09-23_reproducibilityReport.md`, in the five sections
`/repro-report` asks for. Every count in it comes from `inventory.json` beside it, written by a
new script, `repro_inventory.py`, that walks the tree, the records, the claims, the sidecars, the
manifests, the plan's decision log and the git history — so the report's numbers reach it
through a file, as Rule 1 asks, and the script is the recipe for regenerating them. Section 5,
what does not hold, lists fourteen things, from the checks verifying shape and not content to
the unpinned version of the environment's builder.

## 6. Housekeeping the release needed

- The prior-project prose grep batch 19 recommended was run. Fixed: `.gitignore`'s entries for
  folders this project never had, its wrong installer name and its prior-project batch numbers;
  the root `analysis/claim.md`'s "not yet pinned" environment line. Recorded and left: comments in
  `check_invariants.py` citing prior-project batch numbers and a dead manifest path (one comment
  naming prior-project nodes was rewritten in the methodological-change commit), and a dead
  headline block in the report generator guarded by a file-existence check.
- The hierarchical report rebuilt (build 2, 127 pages; provenance appended). The generator is
  unchanged.
- `/release` and `/repro-report` now name the scripts that implement them; `track-result` names
  its reference; `node.md`'s usage runs. Two methodological-change commits, each stating what
  changed about the method.
- READMEs for `AI-generated/`, `validation/`, `useful-scripts/`, `manuscript/`, `repro-report/`
  and the root updated to what is there.
- `readme-at-start.md`'s status rewritten to lead with the current state and to give the history
  as one line per batch.

## 7. Decisions and their agency

All recorded in plan §4b under 2026-09-23, batch 20. In brief: the manuscript's method-of-work
section and venue-neutral form, `agent-autonomous`, reversible; the reference re-verification
and the Sesay correction, `agent-autonomous`, `agent-retrieved`; the scan's failed verdict left
standing with its examination, `agent-autonomous`; the home path reported not redacted, the
redaction the human's; data permission settled by checking upstream, `agent-retrieved`; the
lockfile-digest fix at the record and at the check, `agent-autonomous`; the planner's missing
v3 route recorded, open; **the push and the citable deposit, the human's.**

## 8. What the human decides now

1. **Push.** Everything is committed and tagged. Before pushing, read
   `AI-generated/validation/2026-09-23_release-scan/summary.json` and the two findings in §2.
   The command, from the repository root:
   ```
   git push origin main --tags
   ```
2. **Redact or not.** The home-directory path in `readme-at-start.md` and plan §4b can be
   replaced by "the vault-collection root" in a one-line commit; the task-log entry and the
   console log are records and would stay.
3. **Deposit.** `/release` asks for a citable, versioned snapshot with a persistent identifier.
   The local tag `v1.0-release` marks the release commit; a GitHub release from it, with the
   repository connected to Zenodo, yields a DOI to cite in the manuscript's §7.
4. **Three open forks**, none for this batch: the stage-1 predictive-family alternative now that
   the year is open (plan §4b, 2026-09-22); the planner's route to a new manifest version with an
   unchanged main path; and the six sibling vaults carrying the unsubstituted settings template
   (T16), which belong to a session at the vault-collection root.

---

*Postscript, 2026-09-23, after this report was written.* The human read the findings in §2 and
§8, confirmed that every ledger row was done, and instructed the push. `main` moved from `c8fd3f5`
to `b5fb880` on the public remote with the tag `v1.0-release` (42 commits). Item 1 of §8 is done;
items 2–4 remain the human's.
