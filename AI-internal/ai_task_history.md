# Task history

One line per completed task. Written by `/log-tasks`; expanded entries in
`ai_task_details.md`.

This log is **part of the published record** of how the work came about. Write entries a
reader who was not present can follow, and keep them honest about what did not work.

- T1 (2026-08-23): Batch 1 of the dengue-forecasting plan — read the five source documents, initialised git and `.venv` (CPython 3.13.7), filled in `readme-at-start.md` and the root `analysis/claim.md`, created `AI-generated/batch-reports/`, and wrote the batch-1 report with ten inconsistencies found in the instructions and source material. No analysis, no data, no Chap. `/validate invariants` passes. **Extended same day**: the two questions the report raised were settled in dialogue — the reference model is `chapkit_ewars_model` and the stability spread is carried forward to the held-out year — and the plan was revised accordingly, the plan as delivered archived, and a §4b decision log opened with the agency recorded on each decision.
- T2 (2026-08-23): Batch 2 of the dengue-forecasting plan — installed and pinned `chap-core==2.1.0` on CPython 3.13.0 as a project-local environment, verified the pin by rebuilding it to an identical 174-package set, and ran one `chap eval` end to end on a pinned five-region example dataset. Established the model contract, the backtest arithmetic and the exact form of Chap's CRPS, and answered all five questions `chapOrientation.md` §5 left open. Report: `26-08-23_b02_chapSetup.md`. `/validate invariants` passes.
- T3 (2026-08-23): Batch 3 of the dengue-forecasting plan — pinned the three Lao files at commit `af362d52` into a write-once, checksum-manifested `Archive/lao-dataset/`, and built the first nodes of the claim tree: `analysis/01_data` with `01_partition` and `02_characterise`. The archived file partitions exactly into a 2 592-row development period and a sealed 216-row 2010 holdout; the schema's stated 2 575 rows turn out to count rows with a non-missing target, not rows in the file. No conversion is needed for `chap eval`. Fixed the backtest scheme — development 3/8/3 with `n_retrain` 1, phase E 3/4/3 evaluating exactly 2010 — against chap-core's own splitter. **The consequential finding is that the headline mean covers 16 provinces and 371 cells, not 18 and 408**: Vientiane never reports and is dropped by Chap's region filter, Xaisomboun stops in 2006 and contributes no evaluable cell. Ten data problems listed as phase-D fork candidates. Report: `26-08-23_b03_dataCharacterisation.md`. `/validate invariants` passes; the tree runs byte-identically twice.
- T4 (2026-08-23): Batch 4 of the dengue-forecasting plan — got `chapkit_ewars_model`, the reference the plan's §2 names, running on the development dataset, so the project has a criterion for the first time. **Mean CRPS 21.891** over 16 provinces and 371 cells, MAE 28.504, 10–90 coverage 0.817 and 25–75 coverage 0.617 — close to nominal in the tails and materially over-dispersed in the middle, which is a specific opening for phase C. Pinned by image digest `sha256:abd8098f` (= source commit `a4c2fa42`); Docker was the whole obstacle and emulated amd64 R-INLA turned out to be fast, 149 s per eight-split backtest against 56 s for a native Python model — so **evaluation is not this project's binding cost**. **The reference is unseeded**: four identical runs gave CRPS 21.71–22.17, so any margin against it under about 0.4 CRPS is inside its own noise; Rule 6 cannot be satisfied for the model the success criterion names, and this was measured rather than assumed small. The unpaired split-level standard error is 5.65 CRPS (26 % of the mean), which bounds an unpaired comparison at roughly half the reference's whole score — phase C must compare paired per-cell instead. Model library: 39 repositories, the last published sweep covers 28 and reports **no metric of any kind**, so no leaderboard exists on Lao data; 23 of the 28 could be run on our file as it stands. Ranked shortlist produced, with spatio-temporal GNNs, the integrated superensemble and a mechanistic thermal backbone ruled out against the data rather than against the budget. Report: `26-08-23_b04_methodSurvey.md`. `/validate invariants` passes.
- T5 (2026-08-26): Batch 5 of the dengue-forecasting plan — the bootstrap, `done — expanded`: no analysis output, and phase A closes. Designed the claim tree the rest of the project is built into: five more children of the root, ten forks, and the file contract between them. **The design decision everything downstream leans on is that the plan's §4b rule about the two kinds of fork becomes a property of the tree's shape** — every fork under `02_setup` re-scores every model including the reference, every fork under `03_models/03_candidate` moves only ours — so a misplaced fork is visible as a misplacement rather than left to be remembered. One code path serves both the main analysis and the stability run: the combination is an environment variable, the main path is combination `main`, and the stability driver calls its siblings' scripts with it set. Perturbation manifest sized at 20 one-at-a-time combinations plus 8 pairs chosen by a rule fixed before tier 1 runs, ~3 h of compute on development and ~1.5 h on the holdout, from batch 4's measured run times and labelled as estimates until batch 12 measures them. **The forecast horizon is removed from phase D's fork list** — forced by the reference model, so a combination at another horizon has no reference to compare against and the root's conclusion is uncomputable there; it is the only item of the plan's fork list reconnaissance removed outright. Population becomes two forks rather than one, the reference is re-scored four times wherever it is scored, and phase C ends on a stated 0.4 CRPS stopping rule rather than a batch count. Twelve concrete batches written into the ledger (8–20); the budget is now expressed in implementation effort rather than evaluation runs, and totals 19 batches plus one optional against the original 21–27. Report: `26-08-26_b05_bootstrapPlan.md`. `/validate invariants` passes.
- T6 (2026-08-26): Batch 6 of the dengue-forecasting plan — the vertical slice, `done — produced`. A probabilistic persistence baseline implemented as a Chap `MLproject` with a `uv_env`, run through `chap eval` on the development file at batch 3's scheme: **mean CRPS 24.879, MAE 29.073, coverage 0.666 (10–90) and 0.491 (25–75), over 371 cells, 16 provinces, 8 splits, in 16 seconds**. Every link in the chain — archived data → development file → model contract → `chap eval` → chap-core's own metric → the contract files batch 5's design specifies — has now been exercised once with a file at every join. **The corridor is bounded on both sides**: untuned regression 44.055 (batch 4), this baseline 24.879, reference 21.891, so a candidate has about 3 CRPS of room. **The two models are miscalibrated in opposite directions** — the reference's central interval is much too wide (0.617 against 0.50) while this one's is nearly exact (0.491) and its tails are much too thin (0.666 against 0.80) — and per-province 10–90 coverage runs from 0.042 in Salavan to 1.000 in four provinces, the first measurement of what the unweighted mean over sixteen unequal provinces hides. **A "trivial" baseline turned out to contain a real judgment call**: two published constructions exist for putting a predictive distribution around a persistence forecast and they disagree; the non-parametric empirical-change form was taken because the parametric one's floor on the mean would set the distribution's width in the majority of cells that are zero, and the choice is now a fork in the plan's inventory. **Rule 3 for models closed**: the model ships exact pins and an interpreter pinned to the patch, and its `uv.lock` was verified byte-identical to the one chap-core's runner actually used, so a model's environment is pinned by a tracked file rather than re-resolved at run time. **Rule 6 satisfied by construction**: the model has no randomness — draws are the empirical quantile function at fixed levels, not samples — verified by two independent runs coming back byte-identical. Not done, deliberately: the paired per-cell comparison against the reference, which needs both models scored from inside the tree and is batch 7's. Report: `26-08-26_b06_verticalSlice.md`. `/validate invariants` passes.
- T7 (2026-08-26): Batch 7 of the dengue-forecasting plan — erecting the claim tree, `done — produced`, and phase B closes. Built twenty nodes from batch 5's design with `/node`: `02_setup` (four forks in sequence, each handing the dataset on, with an assembly step that gives every model one dataset and one set of evaluation flags), `03_models` (two baselines and the external reference), `04_score` (per-cell collection, a weighting fork, the leaderboard and the paired comparison), and the root's `conclude.py`. **`analysis/run.sh` now reproduces the whole reported analysis end to end in about twenty minutes**, eighteen of them the emulated reference. The persistence model moved into its node with `git mv` and reproduced batch 6's mean CRPS to the last digit (24.879338288409706), which is the check that the tree added no arithmetic of its own. Seasonal climatology added as the second required baseline: **24.337**. The reference scored from inside the tree for the first time, four repeats, per-cell mean **22.098** (individually 21.820–22.385). **The batch's substantive finding is what the comparison can resolve.** Pairing cell by cell is two to four times tighter than batch 4's unpaired 5.68 — standard error 1.34 to 2.05 treating the 371 cells as independent, 1.92 to 2.99 clustered by split — but both baselines sit 2.2 to 2.8 CRPS from the reference, so **neither difference clears two standard errors on any reading**. The development backtest resolves roughly **4 CRPS**, wider than the whole corridor from persistence to the reference, and below **0.57 CRPS** nothing can be attributed to a model at all, that being how far the unseeded reference moves against its own repeats. A candidate can top the leaderboard without the evaluation being able to say it beat the reference, and that is now measured rather than feared. Two further findings phase C needs: **at one month's lead a persistence baseline is level with the reference** (16.43 against 16.54), which earns its aggregate advantage entirely at two and three months; and **the reference is beaten by both baselines in the two provinces carrying the most evaluated cases** while winning nearly everywhere else, so the cheapest fork in the project — how the headline mean is weighted — may also be one of the most consequential. Settled two things open since batch 2: **the `environment/` Docker layer builds** (1.62 GB, CPython 3.13.0, chap 2.1.0), and the clean-room check ran inside it — `01_data` and `02_setup` byte-identical, both baselines' per-cell scores exact, two small differences reported rather than waved away, and the structural limitation that a clean room cannot cover the containerised reference. **What went wrong is kept**: an early clean-room attempt mounted the repository writable and broke the host analysis environment, and the repair uncovered that `install-chap.sh` had been *writing* `lock.txt` rather than installing from it — so a rebuild three days after the environment was pinned resolved a different package set while the file claiming to be what reproduces sat unchanged in git. Fixed at the cause; the installer now installs from the lockfile and reports any difference. Five smaller corrections, each found by a run or a check. Report: `26-08-26_b07_erectTheTree.md`. `/validate invariants` passes; `/validate cleanroom` recorded in `AI-generated/validation/26-08-26_cleanroom.md`.
- T8 (2026-08-27): Batch 8 of the dengue-forecasting plan — the candidate contract and candidate 1, `done — produced`, and phase C opens. Built `03_models/03_candidate` with `a_hierNB` and one main-path child under each of its four configuration forks (`01_observation/a_negBinomial`, `02_covariates/a_lagged`, `03_population/a_offset`, `04_fitTime/a_trainOnly`), each fork writing an option specification that the node's own assembler merges into the one configuration file `chap eval` is pointed at. **This settles how model configuration reaches an `MLproject` model**, open since batch 2 and unexercised because neither baseline has any: `--model-configuration-yaml` → `ModelConfiguration` → `model_configuration_for_run.yaml` in the run directory → the `{model_config}` placeholder in the entry points, so a run's configuration is a stored artifact rather than a command line. Confirmed against `chap-models/ewars_plus_template`, which batch 5 flagged and left unread and which also supplied the default covariate pair and lag — its published `laos_eval_config.yaml` names rainfall and mean temperature at `n_lags: 2`. **Rule 6 gets its first real surface**: `analysis/scripts/lib/project_seed.py` reads the one project seed from the settings table that declares it and derives a component seed by BLAKE2b, and the derived seed travels into the model inside the configuration file; verified by two runs and a diff, identical. The model is a hierarchical negative-binomial GLM on three pinned dependencies — population offset, two-harmonic shared season, lagged standardised climate, and province and province-year effects pooled toward a common level — fitted by Fisher scoring with EM updates for the variance components, drawing forecasts from the Laplace posterior, from the prior on the year effect, and from the negative binomial on top. Two seconds to fit, 43 seconds for the eight-split backtest. **It scores mean CRPS 26.100 — last of the four models, worse than both required baselines — and mean absolute error 27.106, the best in the project, ahead of the reference's 28.902.** The centre is right and the width is wrong, and wrong locally rather than on average: two provinces carry 2.4 of the 4.0 CRPS gap to the reference, with the 10–90 interval covering every outcome in Vientiane Capital and an eighth of them in Salavan, because the province-year variance is one number shared by all provinces and so a constant multiplicative width. The aggregate coverage of 0.720 sits between two failures and describes neither. **A result that revises batch 7**: the backtest's resolution depends on the pair being compared, not on the dataset alone — the candidate's per-cell differences against the reference are half as variable as persistence's (clustered standard error 1.11 against 2.99) because the two models are structurally alike and fail on the same cells, so this is the first comparison in the project that clears two standard errors, and what it says is that our candidate is worse. `conclusion.json` now reports `candidate_exists: true` and the headline skill score moved from −0.101 to −0.181, which is the machinery working rather than a regression. Deliberately not done: an autoregressive term on lagged counts, which no fork covers and which is proposed to batch 9 as a fifth fork rather than added silently; and re-running the unseeded reference, which would have replaced batch 7's four repeats with a different draw and moved the denominator of every conclusion. **`check_invariants.py` changed and that is a change to the method**: its `crossing` heuristic used a whitespace class before a trailing comment and `\s` matches newlines, so a modelling constant followed by a blank line and a paragraph of prose was reported as a transcribed value; the separator is now spaces and tabs, which is what the docstring's "trailing comment" means, and nothing that was a finding stops being one. Report: `26-08-27_b08_candidateContract.md`. `/validate invariants` passes.
- T9 (2026-08-27): Batch 9 of the dengue-forecasting plan — candidate 1's forks, swept and promoted, `done — produced`. Built the seven sibling children batch 8 left unbuilt and the two forks batch 8 proposed rather than took silently: `05_autoregressive` (`a_none` | `b_lag3`), the lagged-count term, and `06_yearVariance` (`a_shared` | `b_provinceScaled`), the structural half of the width defect batch 8 diagnosed. `hier_nb.py` gained the code for all six options — a zero-inflation EM step, a two-part hurdle with a penalised logistic presence block, covariates at several lags, a lagged-count column, a refit inside `predict`, and a province-year variance estimated per province — and the rewrite reproduced batch 8's per-cell scores byte for byte, which is a stronger check than the tests that would have been written instead. **Every child of every fork is now built and has been run on development**, one combination each, with everything a combination did not move inherited from `main` through the new `COMBO_BASE` mechanism and recorded as inherited in the file that reports it; the unseeded reference is therefore not re-run under a candidate-internal fork. **Three forks moved**, by a rule (a fork moves only if its best child beats the main path by more than batch 7's measured 0.57 CRPS floor) written after the sweep's numbers existed and **committed before the combination it chose was run**: `01_observation` → `c_hurdle`, `02_covariates` → `c_climateFree`, `06_yearVariance` → `b_provinceScaled`. **The candidate went from 26.100 to 23.698 mean CRPS — the first model of ours to beat both required baselines** — with the best point forecast in the project (MAE 27.569) and 10–90 coverage 0.701 against nominal 0.80. Its paired difference against the reference fell from 4.002 to **1.599 with a split-clustered standard error of 1.551**, so at 1.03 standard errors the development backtest **cannot separate our candidate from the field's own model**; the plan says an honest "we cannot separate these two" is a conclusion, and this is the first time the project has been entitled to one. Skill score −0.181 → −0.072. **The batch's more useful finding is methodological: one-at-a-time fork effects do not add.** A second sweep around the promoted main path re-measures every child — the hurdle is worth 0.601 there rather than 2.115, the per-province annual variance 0.051 rather than 1.870, and dropping the climate covariates changes sign — so three forks worth 4.632 CRPS separately delivered 2.402 together and two of nine children reversed sign. Tier 1 of the phase-D manifest is a one-at-a-time sweep and therefore measures a quantity that does not compose; **tier 2's pairs, which were first in line to be cut, are now the part of the manifest with evidence behind it**. Half of batch 8's width diagnosis holds: Vientiane Capital's coverage came off 1.000 to 0.88 and the candidate now beats the reference there, while Salavan's stayed at 0.12 — a per-province variance widens a province whose variance is genuinely large and does nothing for one whose epidemic years are sharper than any log-scale variance can represent. **Two decisions left visible rather than taken**: the promotion rule was applied once and not iterated to a fixpoint, although two children (refitting inside `predict`, +0.873; the richer covariate set, +0.821) are outside the floor from where the model now stands and would take the candidate to about 22.8 — iterating is greedy coordinate descent on development CRPS, which is the failure phase C warns about and which one held-out year cannot diagnose, and the cost of stopping is stated rather than hidden. **Kept in the record**: the promoted fit runs to its 200-round cap because the convergence test is a maximum over seventeen relative movements and the smallest variances keep it above tolerance long after the parameters have settled — the tolerance was **not** relaxed; a `fit_time = predict` combination leaves no fitted object, since the fit happens once per split inside chap-core's untracked run directories; and the autoregressive premise script first computed correlations of `NaN` by reading `1998-01` as `199801`, fixed at the source and re-run. Report: `26-08-27_b09_candidateForks.md`. `/validate invariants` passes; determinism re-verified `identical` for all three of our models.
- T10 (2026-08-28): Batch 10 of the dengue-forecasting plan — candidate 2, gradient-boosted trees with a probabilistic head, `done — produced`. Built `03_models/03_candidate/b_boosted` and its two forks — `01_features` (`a_lagBlock` | `b_richCalendar`) and `02_head` (`a_negBinomial` | `b_quantileEnsemble`) — with a Chap `MLproject` model on four pinned dependencies, and ran all three combinations on development. **The main path scores mean CRPS 20.771 over the same 371 cells — the first model of ours to beat the reference (22.098) and ahead of each of its four unseeded repeats individually (21.820–22.385)** — against candidate 1's 23.698, climatology's 24.337 and persistence's 24.879. The paired per-cell difference is **−1.327 with a split-clustered standard error of 1.110**: 1.20 standard errors, where batch 9's candidate was 1.03 on the other side, and the first margin in the project to clear the 0.565 CRPS floor the reference's own re-runs occupy. It still does not separate the two models and is not reported as though it did — what changed is the sign of the point estimate, not the resolution. **It is also the first model of ours whose intervals are too wide rather than too narrow** (10–90 coverage 0.825 against nominal 0.80) and **it repairs the failure batch 9 could not**: Salavan, which carried the largest single piece of candidate 1's gap, goes from 55.09 to 35.44, ahead of the reference — because a tree that separates two provinces gives them different widths as a by-product of giving them different means, which candidate 1's constant log-scale width cannot do. By lead time it is 18.36 / 19.25 / 24.69 against the reference's 16.54 / 21.97 / 27.79, losing one month and winning two and three, the reverse of candidate 1's pattern. **Neither internal fork moved**: the richer feature set is worth 0.396 CRPS and the quantile head costs 0.189, both inside the floor, so batch 9's promotion rule — fixed before candidate 2 existed — says no to a number that looks better. **The family fork was deliberately not promoted**, although candidate 2 leads candidate 1 by 2.927 CRPS: batch 5's design assigns the family main path to batch 11, after `c_ensemble` exists, so `analysis/run.sh` still reproduces candidate 1 and candidate 2 runs under its own combination `family_boosted`; what the rule would decide today is recorded so batch 11 cannot decide otherwise silently. **The batch's methodological centre is a prediction registered before the run and measured after it.** The head fork computed its premise before anything was fitted and wrote down that every ladder level below the target's 56.3 % zero share cannot move off zero — at those levels the majority of residuals sit exactly on the pinball loss's kink, where the gradient says nothing about direction. Eight levels were named; `check_head_premise.py` afterwards found **seven flat at zero across the whole file** (largest count returned anywhere: 0.3) and the eighth flat in Vientiane Capital, the one province that never reports a zero and whose observed median is 109 cases. Confirmed against both of scikit-learn's independent implementations and a synthetic two-group problem, so it is the data's 56 % ties and not a hyper-parameter mistake. **The failure is kept, not repaired**: the fix is a hurdle over the ladder, which would be a third child of `02_head` rather than a change to this one. The two heads fail in opposite directions — the ladder has the project's closest interval coverage (0.798) and its worst point forecast (MAE 33.020) — which is §2's "a model that wins on mean CRPS while being badly calibrated has not won" with the halves swapped. **scikit-learn enters the project** in one model's environment, pinned by the `uv.lock` beside it, because gradient boosting is not two functions and a hand-written version would be unreadable *and* incomparable with anything published — the opposite of the case for hand-writing candidate 1's fitter. Rule 5 then forbids pickling the result, so **the fitted ensembles are stored as JSON this repository's own code walks**, verified readable with scikit-learn blocked, and because a second prediction path can disagree every fit checks it against scikit-learn's own prediction and fails otherwise — largest difference 0.0 on all three combinations. Batch 21's finding that a rule selecting on development CRPS cannot see whether the model it selects can be inspected is the direct ancestor. Boosting hyper-parameters are a logged decision rather than forks, with the round count chosen by a **time-ordered** early-stopping split, because a rule scored on rows interleaved with the training rows chooses a model for a different problem than the backtest poses. **Two defects fixed at their cause.** All three comparison figures built their colour maps by zipping the models present against a four-entry list, so a fifth model would have drawn as *no series at all* in two of them, silently, and raised a `KeyError` in the third — the invariant checks could not have seen it, since the figure would still exist with its plotted values beside it; replaced by `analysis/scripts/lib/palette.py`, keyed on the model's own name so a model keeps its colour across combinations, and `main`'s figures redrawn with no plotted value changed. And `candidate_fork_sweep.py`, hardcoded to candidate 1 around `main`, now takes `--candidate`, `--base` and `--inherit-from` — the third because a swept row is *measured against* `family_boosted` and *inherits* from `main`, and collapsing the two would make one of them wrong. **Kept in the record**: the premise check's first version bounded each booster over every possible input and reported zero flat levels where seven plainly are, answering a different question than the premise asked; the registered prediction over-reached by one level; a zsh loop wrote one mangled line into two provenance records, caught by `/validate invariants`; and `boosted.py` changed after the evaluations had run, so all three were re-run and came back identical. Report: `26-08-28_b10_boostedCandidate.md`. `/validate invariants` passes; determinism `identical` for all four models, the check extended to name candidate 2 by its own path because the family node would have run candidate 1 twice.
- T21 (2026-08-27): Batch 21 of the dengue-forecasting plan — the greedy branch, `done — produced`, **on a git branch named `greedy` that is never merged**. The number follows the batch, not the sequence: batches 10–20 are still open, and this one was taken out of order because the human asked what iterating batch 9's promotion rule would have produced. Batch 9 applied that rule once and stopped, on the grounds that iterating is greedy coordinate descent on development CRPS; the human settled it both ways — the main path does not iterate, and the iterated path runs on a branch so that what stopping cost is measured rather than estimated. `AI-internal/useful-scripts/greedy_iterate.py` executes the rule rather than leaving it to a reading of a table: each round writes down what every fork's best child scored and which forks it moves, *before* it touches the tree. The rule is batch 9's with the "applied once" clause removed and a round cap of 8 added, and it was committed before the first round it decided was run. **Three rounds, then a fixpoint.** Round 1 moved `02_covariates` → `b_rich` and `04_fitTime` → `b_refitAtPredict`, 23.698 → **21.857**; round 2 moved `05_autoregressive` → `b_lag3`, → **21.275**; round 3 swept every fork again and found the best remaining move worth **0.150** CRPS, a quarter of the resolvable floor, so the branch stopped because the rule ran out and not because the cap bound. **The greedy candidate beats the reference model — 21.275 against 22.098, and against each of its four repeats individually — with a skill score of +0.037 where the main line's is −0.072, MAE 26.278, and 10–90 coverage 0.741 against the main line's 0.701.** By lead time it is 17.97 / 20.41 / 25.44 against the reference's 16.54 / 21.97 / 27.79, so it wins at two and three months and loses at one, where the whole remaining gap is. **And it changes nothing the project can conclude**: the paired per-cell difference is −0.823 CRPS with a split-clustered standard error of 1.602 — half a standard error, where batch 9's candidate was 1.03 on the other side, and 50.4 % of cells. The main line's "we cannot separate these two" survives the counterfactual with the sign of the point estimate reversed, which is the strongest available evidence that stopping cost the project nothing it reports. **What iterating cost is paid in the record, not in the score.** The first fork the rule moved is `04_fitTime` → `b_refitAtPredict`, and under `fit_time = predict` the fit happens once per split inside chap-core's untracked run directories: the branch's `fitted_model.json` is a **520-byte stub** where the main line's carries seventeen annual variances, two blocks of coefficients and an EM history. A rule that selects on development CRPS cannot see whether the model it selects can be inspected, and that is an argument for the main line's decision that the score alone does not give. **A second demonstration that one-at-a-time fork effects do not compose, with the sign reversed:** the lagged-count term is worth −0.075 around batch 8's configuration, +0.353 around batch 9's, and +0.582 once the model refits inside `predict` — it and the refit are complements, and no one-at-a-time sweep can see a complement. With batch 9's overstatement finding, **tier 2 of the phase-D manifest now has two independent demonstrations behind it and batch 12 must not cut it**. Salavan is untouched by two more rounds of selection (47.4 against the reference's 38.4, coverage 0.21), so the remaining gap is where batch 9 said it was. **Kept in the record**: the loop ran unattended between commits, so the tree's main-path markers moved twice inside one commit and round 2's per-combination results were replaced by round 3 before any commit held them — batch 9's hand-run rounds are recoverable at `49825b5` and these are not; and **`verify_model_determinism.sh` has reported `status: differs` since `models.csv` gained its `scored_under_combo` column**, because the check compares that column across two scratch combinations whose names differ by construction — the per-cell scores and fitted objects match in every case, which is what batch 9's report claims in words, but its parenthetical citation of the file's status (and T9's above) is wrong. Nothing was changed: the cause is in the check's specification, not in the models, and the repair is the human's. Report: `AI-generated/batch-reports/26-08-27_b21_greedyBranch.md`, **on branch `greedy`**; what it settled is in the plan's §4b, here on `main`. `/validate invariants` passes on both branches.
- T21 continued (2026-08-27): the follow-up batch 21 raised and the human settled the same day — **`verify_model_determinism.sh` repaired on `main`**. The defect: batch 9 added a `scored_under_combo` column to `models.csv`, and the check ran its two passes under scratch combinations named `determinism_<model>_1` and `_2` and compared that file byte for byte, so it was comparing a field whose value is the pass's own scratch name. `identical` at `509d458` (batch 8), `differs` at `dec4116` (batch 9) and on every run since, for every model, whatever the models did — **Rule 6's only instrument stuck on red**, which is the failure `AGENTS.md` §5 guards against arriving inside the guard. Demonstrated rather than inferred: two hand-run passes of the persistence baseline differed in exactly one line of one file, in exactly that field, while the 372-line `metrics_cell.csv` and the fitted object were byte-identical. **The repair removes the difference at its source rather than the comparison**: both passes run under one combination name, pass 1's three files are copied aside, pass 2 overwrites them in place, and the copy is compared with what replaced it — so nothing is exempted and a genuine difference still fails the check. Dropping the one column was rejected because an exemption list is where a second exemption can later be added unnoticed; leaving it documented was rejected because a check nobody can read at a glance is not a check. Re-run at `6a71a67`: **`identical` for persistence, climatology and the candidate** — identical per-cell scores, model listings and fitted objects. Batch 9's report gained a dated correction section saying that what §10 claims in words held throughout and only its citation of the file's status word did not; the determinism README's "Currently" section, stale since batch 7, was refreshed. Agency: the defect and the three candidate repairs were the agent's, the choice among them and the timing the human's (`agent-on-human-assessment`). `/validate invariants` passes.
- T11 (2026-08-28): Batch 11 of the dengue-forecasting plan — candidate 3, the ensemble, and the close of phase C, `done — produced`. Built `03_models/03_candidate/c_ensemble` and its one fork `01_weighting` (`a_equal` | `b_crpsWeighted`) as a **linear opinion pool** over the two candidate families and both required baselines, and ran both combinations on development. **The main path scores mean CRPS 18.817 over the same 371 cells — the project's best, against the reference's 22.098, candidate 2's 20.771, candidate 1's 23.698, climatology's 24.337 and persistence's 24.879 — and the family fork was promoted to it, so the reported conclusion turns positive for the first time at skill +0.1485.** The paired per-cell difference against the reference is **−3.282 with a split-clustered standard error of 1.726**: 1.90 standard errors, better in six of the eight splits, and better than each of the reference's four unseeded repeats individually by 3.00 to 3.57 — nearly six times the 0.565 CRPS floor, and **still short of separating the two models**, which the report says plainly. **The model contains no member code.** Each member is run through **its own Chap entry points**, with the command string read out of the member's own `MLproject` and the placeholders filled in, so there is one copy of every member's code in the repository at the node that owns it; the membership is a stored document (`members.json`) whose path and sha256 are in the model configuration, and the model refuses to pool anything if the two disagree. `prepare_members.py` discovers members by globbing for `MLproject` and, for the two that take configuration, runs **that family's own fork main-path children and that family's own assembler under the running combination** — which is what makes a phase-D perturbation of a member's fork move the pool's member with it. The cost, stated rather than discovered, is that the pool's environment is the **union** of its members', checked pin by pin at the top of the run. **The batch's two most useful results are both negative.** First, a prediction registered before the model ran — that an equally weighted pool would score *worse* than its best member, because half its mass sits on the two required baselines — is **false**: the pool beat its best member by 1.954 CRPS. The prediction reasoned about where the forecasts sit and not about how wide they are; every member but candidate 2 under-covers (10–90 coverage 0.650–0.701 against nominal 0.80) and pooling widens. Its other two clauses held. Second, **fitting the pool's weights cost 4.021 CRPS**: `b_crpsWeighted` minimises the pool's own CRPS exactly, from the sample identity `CRPS = E|X−y| − 0.5E|X−X′|`, on four three-month blocks held back inside the training frame, and found the true minimum there (14.227 against 17.026 at equal weights) — but it put 0.953 of the pool on candidate 1, which was best on the validation year (2007) and is the **worst** of the three non-baseline members on the evaluated period (2008–09). One year of held-back data does not say which member will be best on the next two, which is the plan's phase-C warning happening inside the tree with both sides in one table. Two of that child's three registered predictions held; the third understated the loss and is reported as two-for-three. **The pool is verified by a second path**: rebuilt from its members' own stored `eval.nc` files, pooled at the same weights and scored with chap-core's own CRPS, it gives 18.801 against the 18.817 it scored — the 0.016 residual being the sampling error of the allocation — and the members' scores by that path reproduce their leaderboard rows exactly. Members are matched to evaluations **by configuration hash, never by combination name**. **The winning model is the most over-dispersed in the project** (10–90 coverage 0.863 against 0.80, 25–75 coverage 0.749 against 0.50), and the innocent explanation was measured and rejected: on a target that is 56 % zeros a model with a large atom at zero has a degenerate central interval, but the pool's quartiles coincide in 24 % of cells against 41–55 % for three of its four members, so it over-covers while being *less* exposed to the artefact. Per province it is best in the two highest-burden ones and beaten in three of the next four, winning only **43 % of individual cells** while being 3.282 better on average — a hedge. It wins at every lead time including one month (14.66 against the reference's 16.54), which neither candidate could, and it gets that from persistence, the crudest model in the project and the best single member in Vientiane Capital. **The family fork moved by a rule written after the family leaderboard existed and committed before the promoted family ran under `main`** (`AI-generated/candidate-forks/family_rule.md`): batch 9's 0.57 CRPS threshold unchanged, each family compared at its own main path, plus a calibration veto that did not fire. Candidate 1's `results/main/` were **removed** rather than renamed, as batch 9 removed demoted children's, and it re-ran under `family_hierNB` with **identical per-cell scores**. **Phase C is closed although its stopping rule permits another candidate batch** — 1.954 CRPS against the 0.4 threshold makes one admissible, not required — on the judgment that the stability answer is worth more than a better score, with the reversal offered to the human. **Kept in the record**: the equal-weight fork child's first version read `04_score`'s leaderboard for its premise, a circular dependency invisible under `main` and fatal under a scratch combination, caught by the determinism check; `check_pool.py` made the same mistake one step later and now scores the pool from its own stored evaluation; and `train.py` changed after the three evaluations had run, so all three were re-run and every figure came back identical. `family_leaderboard.py` is new machinery — what each family scores at its own main path, found by matching chosen child nodes rather than naming combinations. Report: `26-08-28_b11_ensembleCandidate.md`. `/validate invariants` passes; determinism `identical` for all five of our models, and the check now names **every** candidate by its own node path, because naming candidate 1 by the family node would have run the pool twice under the name `hier_nb` after the promotion.
- T12 (2026-08-29): Batch 12 of the dengue-forecasting plan — `/perturb plan`, the opening of phase D, `done — produced`. Built `analysis/05_stability`: `lib/inventory.py` (the fork inventory, walked off the tree rather than listed), `measure_step_costs.py`, `plan_manifest.py`, `run_manifest.py` (the combination driver) and `collect_conclusions.py`. **Created the nine fork children the tree had named in prose and never carried** — the second child of each of the four `02_setup` forks, both children of the scoring fork, and the second child of each of the two baseline forks — so that seven of the ten judgment calls phase D exists to perturb are paths in the tree rather than sentences in a `claim.md`. The manifest is **17 forks** where batch 5's hand-written list counted ten, **24 tier-1 combinations**, **8 tier-2 slots** filled by a rule written and hashed before tier 1 ran, and one tier-0 combination that already existed and perturbs nothing. Every cost is measured: model terms from each model's own `run_cost.json`, pipeline terms from `measure_step_costs.py`, which times three steps by re-running them under `COMBO=main` and asks git whether they left the tree byte-identical. **124 minutes on development and 75 on the holdout against a 12-hour budget the agent set — and 89 of the 124 are re-running the reference model**, four unseeded repeats on each of five setup rows, for the one model the plan forbids perturbing. `/validate invariants` gained a **`combos`** check that closes the combination space, and it failed on its first run on a real gap (`family_ensemble`, fifteen directories with no manifest row); the manifest was made complete rather than the check exempted. Planning found two code defects and twelve stale combination directories that batch 14 would otherwise have hit with the holdout that much closer. Nothing in the manifest has been run: one row, the main path, went through the driver to exercise it and came back byte-identical. Batch 22 appended to the ledger between 13 and 14. Report: `26-08-29_b12_perturbationManifest.md`. `/validate invariants` passes.
- T13 (2026-08-29): Batch 13 of the dengue-forecasting plan — `/perturb run` for the setup and scoring rows, `done — produced`, and the first batch of phase D that runs anything. Built the seven children the tree had named in prose and carried empty since batch 12 — `01_population/b_backCast`, `02_trainingWindow/b_from2004`, `03_provinces/b_reportingOnly` and `c_mergeVientiane`, `04_retrain/b_everySplit`, and both weighted children of the scoring fork — and ran their rows, so **8 of the 24 tier-1 combinations have conclusions**. **The five `02_setup` forks do not move the conclusion**: skill spans +0.1266 to +0.1861 around the main path's +0.1485, every gap smaller than the reference's own 0.57 CRPS re-run spread. **The single `04_score` weighting fork moves it four times as much** — +0.2288 population-weighted and +0.2320 case-weighted — from re-weighting a stored per-cell file and re-running no model at all, so the cheapest row in the manifest at thirteen seconds is the one the conclusion is most sensitive to. **Under case weighting the required persistence baseline beats the model this project reports**, 86.598 to 88.484, and the pool's 10–90 coverage inverts from over-dispersed to under-dispersed, 0.863 to 0.701: it is too wide on the quiet months that dominate the unweighted mean and too narrow on the outbreak months that dominate this one. **Three of the five setup rows move the score by moving the *reference* rather than our model** — dropping the two unevaluable provinces costs the reference 1.05 CRPS and our pool 0.03 — which a headline reporting only our own score would hide entirely, and which is an argument for the ratio §4b had already fixed. Archived `Archive/lao-population/` (World Bank `SP.POP.TOTL`, 1990–2021, fetched by script and checksummed) for the back-cast, and established from it that **the dataset's population column does not have the level its schema claims**: it sums to 4 961 076 against a stated 2020 reference whose national total was 7 346 533, matching the country around 1995 — the third statement in that schema found not to describe the file. Where batch 12 found two defects by planning, this batch found three by running: `conclude.py` naming a required baseline as the project's model on both scoring rows; **the reference model crashing about once in a hundred jobs**, now retried up to three times with `attempts_per_repeat` recorded; one container serving all four repeats and slowing monotonically until it disconnected, now one container per repeat; and **a failed re-run leaving a results directory that looked complete** while spanning two commits, now cleared before writing — the most dangerous of them, because it produces a wrong number no check was looking for. Batch 12's cost model predicts the total to 2 % (6 041 s planned against 5 901 s) and every individual row wrongly, ratios 0.577 to 1.851, so the cut order within a kind is ranked on a constant; nothing was cut, so nothing rests on it. `plan_manifest.py` now refuses to apply the tier-2 rule until every tier-1 row has been attempted, which is a change to *when* the rule is read and not to the rule — `tier2_rule.md` and its sha256 are unchanged. Report: `26-08-29_b13_setupAndScoringRows.md`. `/validate invariants` passes: its `provenance` check failed on 377 artifacts first and was satisfied by writing records, not by weakening it.
- T22 (2026-08-29): Batch 22 of the dengue-forecasting plan — `/perturb run` for the two baseline forks' children, `done — produced`. Built `01_persistence/b_negBinomialFloor` (the parametric construction: a negative binomial about the last observed count, mean floored at 0.2, dispersion by maximum likelihood from the last five observations, every constant taken from the KIT baseline rather than chosen here) and `02_climatology/b_frozenWindow` (the seasonal table estimated once from the training frame and held fixed), each as its own Chap contract directory with its own `uv.lock`, both resolving to the same six packages as their siblings'. Ran both rows: **10 of 24 tier-1 combinations have conclusions and the planner reports zero unbuilt children**. **The two rows are the extremes of the stability set.** Freezing the climatology window costs that baseline 0.532 CRPS — inside the 0.565 floor — and the reported model 0.0025 of skill, the smallest move of any row. The parametric persistence construction is **4.181 CRPS better** than the main path's (20.698 against 24.879) and **beats the reference model** (paired −1.400 ± 1.424), so the reported analysis stands on the worse of two published constructions of a baseline the plan requires — and the pool that contains it is **better off for that**: skill falls to +0.1206, the largest move of any row and the first downward one clearing the noise, because a linear opinion pool profits from its members disagreeing and the sharper member narrows it. With batch 11's result that fitting the weights costs 4.021 CRPS, that is two independent demonstrations that this pool's win comes from disagreement rather than member quality. The fork is **not promoted**: phase C is closed and the manifest was frozen before the row ran. Two more fork-blind globs found by building rather than running — `prepare_members.py` would have made the reported pool a **six-member pool with two persistence baselines and two climatologies**, silently, under `main`, and `01_collect` inherited from `COMBO_BASE` per node rather than per fork — both fixed, both leaving every existing result byte-identical. Report: `26-08-29_b22_baselineForkRows.md`. `/validate invariants` passes; determinism `identical` for all **seven** models, each now named by its own leaf node.
- T14 (2026-08-31): Batch 14 of the dengue-forecasting plan — `/perturb run` for the candidate and family rows, then tier 2, `done — produced`, and the batch that completes phase D's development set. Ran the twelve candidate rows, the two family rows and all eight tier-2 pairs, so **32 of the manifest's 33 rows have conclusions**; the thirty-third is `family_ensemble`, the main path's own choice under its own name, and `collect_conclusions.py` now says that rather than reporting it as a row waiting for a batch. Across the 32 the reported skill spans **−0.0724 to +0.2320** around the main path's +0.1485, our model beats the reference on **27** and both required baselines on **27**, and 10–90 coverage runs **0.458 to 0.920** against a nominal 0.80. **The headline finding is a ratio, not a score: the choice of model family moves the reported conclusion twenty-seven times further than any choice inside a family.** Swapping the reported pool for candidate 1 costs 0.2209 of skill and for candidate 2 0.0884, while the eleven forks inside those two member families — the observation model, covariates, lags, population, fit time, year variance, boosted features and head — span **18.638 to 18.933 CRPS**, a range of 0.295 against the reference's own 0.57 re-run spread, so nothing in that block can be attributed to a model at all. Phase C spent batches 8, 9 and 10 selecting among analyses this evaluation cannot separate, and batch 21 spent a branch iterating that selection. The mechanism is the pool, measured rather than asserted from phase C's own committed sweeps: candidate 1's observation model is a 0.601 CRPS swing to candidate 1 alone and a 0.102 swing to the pool because three of four members did not move, and **candidate 2's quantile head makes candidate 2 worse while making the pool better**, so the damping is not simple scaling — batch 22's better-member-worse-pool result from the other side. **The one exception is the pool's own weighting fork**, the only candidate-internal fork not inside a member: fitting the weights on a held-back year costs **0.1820** of skill, takes the reported model to 22.838 CRPS *behind* the reference, and drops its paired comparison to 0.48 standard errors — the second-largest single-fork move in the project. **Tier 2 earned its place: the pairs are not the sum of their parts and the largest interaction exceeds both main effects behind it.** `collect_conclusions.py` gained `delta_skill_additive` and `interaction`; across the eight pairs the interaction runs **−0.1033 to +0.0424**, and the extreme is `provinces_reportingOnly × aggregate_caseWeighted` — the province filter is worth +0.0376 alone and case weighting +0.0835 alone, so additively +0.1211, and together they come to **+0.0177**. Both work by re-weighting what the mean is over, so taking both does not do it twice. Third demonstration that fork effects do not compose, after batches 9 and 21, and the first on the reported conclusion; a one-at-a-time stability report would have been wrong about that pair by two thirds of the whole main-path skill score. **Case weighting is the one condition under which a required baseline beats the reported model**, in all five combinations it appears in, and the two rows pairing it with a model swap have the worst calibration in the project (coverage 0.458 and 0.468). **Four defects, three named in the ledger and a fourth found by running into it, all of them the same fork-blindness** — a step that discovers something from the tree, written when every fork had one child that did anything. `prepare_members.py` ran every member fork's main-path child where the combination had already moved one, which blocked all twelve candidate rows; `conclude.py` read our reported model off `claim.md`'s `main-path`, which does not move with the combination, so every family row would have said `candidate_exists: false`; `03_compare` computed its paired spread unweighted beside a leaderboard that followed the weighting fork, so **batch 13's two weighting rows had been reporting the main path's own −3.282 ± 1.726 against its 0.565 floor under a re-weighted mean** — corrected, they are the strongest separations in the project at 2.87 and 2.30 standard errors; and the driver called a family's `run.sh` after the moved child, which re-runs that fork's sibling when the fork belongs to the family that is running. Also carried out the **assembler lift** batches 10 and 11 both logged and deferred to whichever batch re-ran the combinations those scripts configured: `03_models/scripts/lib/assemble_config.py` is now the one way a candidate family's configuration is assembled and the three families are one-screen runners, verified by every `model_configuration.yaml` and `candidate_spec.json` under `main` coming out byte-identical. Removed the twelve phase-C combination directories the manifest's rows replace, with git as the witness. **Not done, against batch 22's expectation**: the per-split diagnostics gap stays open, because its premise was that this batch touches `chap_eval.py` — it touched the assemblers — and closing it would re-hash the models, the pool's membership and the reported model's configuration, forcing a re-run of the reported analysis between the manifest freezing and the holdout opening. Batch 12's cost model confirmed a second time as right on the total and wrong on every row: 1 293 s against 1 224 for tier 1, 5 703 against 6 047 for tier 2, per-row ratios 0.52 to 1.91; phase D has used 3.2 of its 12 budgeted hours. Storage measured now the manifest is complete: **git tracks 1.1 GB** and `analysis/` holds 10 GB, the 8.8 GB difference being gitignored `work/` directories nothing reads after a run. Report: `26-08-30_b14_candidateAndFamilyRows.md`. `/validate invariants` passes.
- T15 (2026-08-31): Batch 15 of the dengue-forecasting plan — `/perturb report`, and the close of phase D, `done — produced`. Turned the thirty-two completed analyses into the phase-D result and froze what phase E runs. **The conclusion survives and the reported number is not the middle of what it survives across**: skill runs **−0.0724 to +0.2320** around the reported +0.1485, **which sits thirteenth of thirty-two** — twelve reasonable analyses conclude a better score and nineteen a worse one — with our model ahead of the reference on 27 and of both required baselines on 27, and the failures structured rather than scattered (the five rows the reference wins are exactly the five that replace our model or refit its weights; the five a required baseline wins are exactly the five that weight the headline mean by cases). **The plan's "most valuable single output" is now one file and one figure**: `results/sensitivity_by_fork.csv` and `fig_fork_sensitivity.png` say that **six of the seventeen judgment calls move the conclusion further than the reference model moves on its own and eleven do not** — family 0.2209, pool weighting 0.1820, headline-mean weighting 0.0835, province filter 0.0376, persistence construction 0.0279, training window 0.0219 — and **nine of the eleven below the line are the candidate-internal forks phase C spent three batches selecting among**. **The yardstick is measured, not chosen**: the reference is unseeded and was scored four times, and our model's skill against those four spans **0.0218**, which is how far the reported conclusion moves when nothing about the analysis changes; the CRPS floor of 0.565 could not have served, because the conclusion is a ratio and the weighting fork makes CRPS non-comparable across six of the 32 rows. For the same reason **mean CRPS is summarised within a weighting and never across one** (18.55–23.70 unweighted, 28.58 population-weighted, 88.48–112.26 case-weighted), which is the first time the project's reason for reporting a ratio has been load-bearing. **`run_manifest.py` joined `run.sh`**, so `analysis/run.sh` now reproduces the distribution as well as the reported result at about four hours rather than twenty minutes — safe because the question that had to be answered first was answered by running it: re-planning against a completed tier 1 returns `manifest.csv` **byte-identical**, and the node's order now runs plan → tier 1 → collect → plan → tier 2 → collect so the frozen pair rule can select from a tier 1 that exists even on a cold run. **The phase-E set is frozen and committed before the year is opened**: 33 rows under `__holdout` names at an estimated 2.07 h, each carrying the development skill score, CRPS, reference CRPS, coverage and model it is to be reported beside, because freezing the set and assembling the development half of the comparison afterwards would leave the comparison selectable after the fact even though neither half was. `check_invariants`' `combos` check now reads both manifests, extending the closed combination space rather than relaxing it. **The first twelve claims** are in `Human-AI-collaboration/claims/claims.md`, all of them phase D's, every pointer resolving. Two findings kept: **`plan_manifest.py` documents a `--freeze-check` flag that does not exist** and could not work as described — it writes the rule and its hash from one constant, so the comparison can only succeed; what evidences the freeze is git, and the check the flag describes is now enforced by `freeze_holdout_manifest.py` — and **the completed development manifest took 3.66 hours, not the 3.2 batch 14 reported**, summed from `run_status.csv`, with planned and actual agreeing at 7 469 s over the rows the manifest costed and per-row ratios from 0.52 to 1.91. No model was re-run and no row re-run to report it, because the reference is unseeded and re-running would move the denominator of every number reported. Report: `26-08-31_b15_perturbationReport.md`. `/validate invariants` passes.
- T16 (2026-08-31): Batch 16 of the dengue-forecasting plan — the holdout, opened once, on the frozen manifest, `done — produced`, and the batch that gives the project its answer. **Thirty-two of the frozen set's thirty-three rows ran on 2010** — every row with a development twin, the thirty-third being the main path under a second name that development did not run either — in **2.39 h** against the 2.07 h they were frozen at, ratio 1.15, none failed. **The reported linear opinion pool scores 76.731 mean CRPS against the reference model's 84.026, a skill score of +0.0868 where development read +0.1485**, and still beats the reference and both required baselines on a year it had never seen; 2010 is about four times the CRPS of the development period for every model in the comparison, which is what reporting a ratio exists to control for. **The news is the distribution, not the win.** Skill runs **−0.5038 to +0.2026** on the holdout against −0.0724 to +0.2320 on development — a range of 0.706 against 0.304, more than twice as wide — the reported analysis moves from thirteenth of thirty-two to eighteenth, six analyses fall below zero where none did on development, and **28 of the 32 scored worse on the year they had not seen**, median −0.056. **The Spearman rank correlation between the two orderings is +0.396**: ranking these analyses on development was a weak guide to ranking them on 2010, which is the question a development set is used for. **The fork ranking transfers better than the analysis ranking**, at +0.679, with **14 of 17 forks agreeing** on whether they clear their own dataset's noise band (0.0218 on development, 0.0140 on the holdout, each measured from that year's four repeats of the unseeded reference) — but the order reverses at the top: the **province filter goes from 0.0376 to 0.2696, the largest fork effect anywhere in the project**, the model family halves from 0.2209 to 0.0949, the pool's own weighting fork collapses from 0.1820 to 0.0121 and falls below the band, and candidate 1's covariate set crosses it the other way. **And the fork that grew moves the reference model rather than ours**: under `provinces_reportingOnly` our pool goes 76.73 → 76.56, inside the noise, while the reference goes **84.03 → 64.72**, so the analysis that ranked fourth of thirty-two on development — highest of every analysis that does not re-weight the headline mean — is twenty-ninth on the holdout, and its case-weighted pair is the worst of all thirty-two at −0.5038. A system optimising freely against a development set would have taken that row. The reported model's over-dispersion does not survive the change of year either: 10–90 coverage 0.863 on development against nominal 0.80, **0.755** on 2010, and 0.210 to 0.854 across the set. Tier 2's largest interaction is **−0.2876**, again bigger than either main effect behind it. **Mechanically, the holdout is reached by one switch.** `01_data/01_partition` gained `open_holdout.py`, which reassembles the archived file from the development file and the sealed one **in the source's own line order** and verifies the result byte-identical — so the node that was the only one licensed to read the whole record is still the only one that does, and no case value of 2010 is parsed by the script that opens the year. Which of the two files a combination reads, which backtest scheme it runs under and which span its province and training-window forks call "evaluated" are all answered by the `__holdout` suffix in `analysis/scripts/lib/combos.py` and nowhere else; six setup scripts and `conclude.py` read it rather than carrying a copy, and every development setup spec was re-derived and moved only in the descriptive fields it gained. `run_manifest.py --dataset holdout` runs the set through the tree's own scripts, its one different row being the main path, which on development runs `conclude.py` alone and here runs the whole pipeline — expressed by moving no fork and opening the setup gate, not by a second list of the pipeline kept in the driver. **The tree gained no node**: `analysis/README.md` had reserved `06_holdout`, and the holdout turned out to be the stability question asked of a second year over the identical set against the identical yardstick, with the frozen manifest already at `05_stability`. **Three things kept rather than tidied.** The driver began re-running `main__holdout` on its first pass over the whole manifest — the reference is unseeded, so a second pass would have replaced the denominator of every reported number with a different draw; it was stopped at the persistence baseline, those files restored from the previous commit, the reference never reached, and the guard is now in the driver, which reads `run_status_holdout.csv` and refuses a row it already records. `freeze_holdout_manifest.py` was writing HEAD into `frozen_at_commit`, the field that evidences its own freeze, overwriting it on every run; it now reads the commit that *adds* the manifest, `937fd5c`, from git — safe to change only because the manifest itself comes back byte-identical after the whole set has run, checked before and after. And `fig_fork_sensitivity.py` had been stating "Six of seventeen" in a title rather than counting its own bars, which is the transcription `AGENTS.md` §1 is about; it now counts them, which it had to for the figure to say the right thing on the second dataset. The two ranked figures moved into `scripts/lib/stability_figures.py` with one-screen runners as the steps, because `/validate invariants` requires a plotting script named for each figure and weakening it is forbidden. **The root node's `claim.md` had been stale since batch 7** — its answers still said the project had no candidate model — and is rewritten with both halves of the answer. **Nine claims added**, C13–C21, all phase E's, and C1's scope corrected where it said the holdout half was not yet run. `analysis/run.sh` is now about six hours and reproduces both distributions. Report: `26-08-31_b16_holdout.md`. `/validate invariants` passes.
- T17 (2026-08-31): Batch 17 of the dengue-forecasting plan — the claim collection completed and the hierarchical report built, `done — produced`. **Eighteen claims added, C22–C39, taking the collection to 39.** The file had a shape problem rather than a size one: batches 15 and 16 had written twenty-one claims and every one of them was about the perturbation set or the held-out year, so the collection recorded how far the conclusion survives its alternatives without recording the conclusion, and recorded that the model family is what the result is sensitive to without recording what the families scored. The new claims are phases A, B and C: the development headline (**+0.1485**, 18.817 mean CRPS against 22.098, better in six of eight splits) and the fact that its margin — **3.282 CRPS against a split-clustered standard error of 1.726**, 43.1 % of cells won — cannot separate the two models; the reference model's own **0.565** CRPS re-run floor and why the reported figure is the mean of four repeats; what pooling bought (**1.954** CRPS over its best member, against a premise registered before the run that predicted it would land *between* the best member and the members' mean) and what fitting the pool's weights cost (**4.021**, enough to lose to the reference); candidate 1, which never beat the reference at −0.0724 and is the lowest of the thirty-two development analyses, and candidate 2, which beat it alone at +0.0601; that the reported model is **cheaper** than the model it beats, 59.5 s against 241–285 s per repeat; that both required baselines lose to the reference, so the baselines are not the bar that binds; what the metric is a mean over (**16 provinces, 371 of 408 cells**); **three statements in the dataset's own schema that do not describe the file** — the row count, the rainfall unit by a factor of 30.4, and the population level by 1.48; the zeros and the declining reporting completeness; the partition verified rather than assumed; the backtest scheme fixed before any model ran; the byte-identical re-runs of all seven models we wrote against a reference that cannot be seeded at all; and the cost model tested as a *prediction* for the first time on the phase-E half — right at the total at 1.15, wrong on the rows again at 1.97. **One discipline point changed what several claims say: a claim states the figures a file holds and never a ratio between two of them that no file computes.** `readme-at-start.md` says the pool sits 1.90 standard errors from the reference; nothing stores that ratio, so asserting it in a claim would write a number computed in the agent's context into the record — the failure `AGENTS.md` §1 describes, arriving in the file whose whole job is to bind statements to results. **The hierarchical report exists for the first time: 1 175 pages.** The tree supplies the upper levels — 69 node pages, each now also carrying the claims from the collection that rest on it, which is where Rules 8 and 9 meet — and below them run four more, for each of the **65 scored combinations on both datasets**: the conclusion as `conclusion.json` states it, the national mean each model is reported at with the same mean cut by split and by horizon, that mean by province, and each province month by month down to the per-cell CRPS, the count observed, and whether the outcome fell inside each model's 10–90 interval. **Every number on every page is displayed from the file the analysis wrote and nothing is recomputed**, so the report cannot disagree with the analysis; the cost is that it can show only what was stored, which is where the per-split-per-province diagnostics gap batches 22, 14, 15 and 16 left open becomes visible from the display end. 9 046 links, none broken; two builds into different directories byte-identical; 1.6 s and 18 MB. **Two defects found by building.** The report was listing a `uv`-built virtual environment inside a model's contract directory as **6 117 of `c_ensemble`'s 6 128 "scripts"** — fixed by asking `git ls-files --others --ignored` what the repository declines to version rather than by carrying a skip list, so the report and the method cannot drift apart; and it crashed sorting Xaisomboun, whose mean CRPS is empty rather than zero because it contributes no evaluable cell, where reading the blank as zero would have put a perfect-looking province at the top of every table. `.gitignore` now ignores the report's *contents* rather than its directory, so the build's `provenance.md` can be versioned while the 18 MB of HTML is not; the folder's `README.md` is generated with the report, because a hand-kept description of a generated folder goes stale silently. **Nothing in `analysis/` moved**: after the holdout is opened, a batch that reports what the analysis found must not be able to change it. Report: `26-08-31_b17_claimsAndReport.md`. `/validate invariants` passes. **Extended after the report, with seven decisions the human settled on batch 17's and batch 16's open questions.** The rule batch 17 had drawn for itself — that a claim states the figures a file holds and never a ratio between two of them that no file computes — was **superseded the same day by the human**, who set the line where the arithmetic stops being legible from the claim itself: *a claim may state what follows trivially from the figures it cites, a ratio or a ranking, and may not state anything that needed a step nobody ran*. C23 now gives the paired difference, its split-clustered standard error **and the 1.90 standard errors they come to**, and is `agent-on-human-assessment`; the rule is in the claims README, where a session writing claims will meet it. Batch 16's two questions closed: **the case write-up's judgment about where this setup was more trouble than it was worth is the agent's**, offered back for comment, and batch 19 labels it `agent-autonomous` rather than jointly held, because the delegation is the human's decision and the verdict is not; and **`analysis/run.sh` is not split**, so it goes on reproducing both distributions and batch 18's clean-room check is a six-hour run. Four more closed phase E: **batch 20 is confirmed rather than cut**, being the only thing in the plan that bears on whether the holdout spread is about 2010 or about one-year backtests, and — the agent's own call — **it moves ahead of batch 19, so the remaining order is 18, 20, 19**, because an external check landing after the case study is a result the case study cannot use; **a git remote is created after all**, reversing §10, at `github.com/sandvelab/veridical-agentic-dengue-laos` (owner the human's, name delegated and chosen here), pushed as the **last** step of batch 19's `/release` after the secrets and data-permission scan and both validations, with the agent asking before the push — measured as feasible first: `.git` is **131 MB** packed and no tracked file exceeds 50 MB against a 1.6 GB working tree; the **target venue is *PLoS Computational Biology***, which had been recorded as not fixed in the source material; and **what `/validate outsider` finds in the instruction files is fixed by the agent and committed as the Rule 4 methodological change it is**, rather than brought back for approval first. All seven are in the plan's §4b with their agency, and `readme-at-start.md`'s remote, venue, status and "left to the human" entries are corrected to match.
- T18 (2026-09-01): Batch 18 of the dengue-forecasting plan — the clean-room, the outsider check and the plan's own drift, `done — produced`, with the clean-room split out rather than claimed. **Both `/validate` checks that had never been run here found more than the sixteen batches before them reported between them, and every finding was the same shape: a statement true as prose and false as behaviour.** `/validate cleanroom` found, before executing a line, that **the holdout seal was versioned and therefore sealed every clone** — a fresh checkout skipped all 32 phase-E rows and reproduced `holdout_distribution.json` byte-identically while running none of the analysis behind it, a false reproduction indistinguishable from a true one, and three places asserted the opposite in so many words. The seal now takes two conditions and needs both: the versioned status row, so forcing a re-run still shows in git, and a gitignored `.holdout_opened` saying this working tree opened the year. On what the run then reached — the whole reported main path and 8 of 32 stability rows before the session was cut off — **every model this project wrote reproduced its mean CRPS to the last digit** (pool 18.816872064690028) while the unseeded reference redrew and moved the headline skill +0.1485 → +0.1550, the 0.171 CRPS behind that being a quarter of the floor below which nothing is attributable. Two things found on the way: **the resolvable-difference floor is itself a random variable**, 0.565 archived against 0.449 re-run, so "nothing below 0.57 CRPS" is a statement about one sample of four repeats; and the clean-room wrote four `member_selection.json` files the archive lacks, so re-running does not quite reproduce the archive. `/validate outsider`, run for the first time, put two fresh agents on disjoint tasks and found **three wrong counts — "eleven forks" for eight forks and their eleven combinations, "nine of the eleven" below the noise band for eight, and 0.514 rounded up to 0.52 — one of them in claim C3, which batch 19 writes the manuscript from**; and **six provenance records naming a commit orphaned by a history rewrite**, which `git cat-file -e` passed because orphans stay in the store and a local clone hardlinks it, and which would have vanished on batch 19's push. The anchored `commit:` pattern had separately skipped `conclude.md` entirely for annotating its hashes, so **the headline result's record was the one the check never read**. The root `README.md` described batch 1 through phases B, C and D and its `conda` build command never worked. The plan's drift is measured by script: **89.1% of delivered lines survive byte-for-byte** while the document is five times its delivered length, the aim survives at 1.000, and **145 of 169 recorded decisions were `agent-autonomous` against 15 `human-set`**. Three batches added — 23 (16 stale provenance script hashes, `conclude.py` worst), 24 (the frozen phase-E manifest is recomputed on every run), 25 (finish the clean-room) — and the human settled that **the perturbation set stays at 32** — which also removes batch 24's practical urgency, since the tree does not grow again before release. **Extended after the report**: the human asked for the §3 ambiguity to be written up rather than left, so **§3 now says which artefact its freeze binds** — `manifest_holdout.csv` and nothing else. The tree and the development manifest may still grow, because `/validate invariants` requires every non-main child to have a development row and the two rules would otherwise contradict each other; a development row added after the opening has no holdout twin, can never acquire one, and is reported as an unpaired analysis rather than joining the 32. `agent-on-human-assessment` — the narrow reading is the agent's, the decision to write it into a non-negotiable is the human's, and §10 reserves the latter.
